name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
      - feature/research-simulation-demo
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Build demo data
        run: npm run build:demo && npm test
      - name: Assemble Pages site
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          cp -R demo _site/demo
          # Ensure relative demo assets resolve under /<repo>/demo/
          printf '%s\n' 'User-agent: *' 'Allow: /' > _site/robots.txt
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
